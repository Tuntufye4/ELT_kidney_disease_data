
version: 2

models:
  - name: kidney_structure
    description: "A kidney_structure data model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

